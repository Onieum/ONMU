# 시작 화면 asset

이 디렉터리의 이미지 파일을 교체하면 원하는 asset으로 launch screen을 커스터마이즈할 수 있습니다.

또는 `open ios/Runner.xcworkspace`로 Flutter project의 Xcode project를 열고, Project Navigator에서 `Runner/Assets.xcassets`를 선택한 뒤 원하는 이미지를 넣어도 됩니다.
