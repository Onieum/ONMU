import 'package:flutter/material.dart';

/// 온챗 탭 — 모임방/채팅방 목록 / route: /onchat
/// TODO(온챗팀): 이 파일을 온챗 목록 화면으로 교체한다.
/// 관련 route: /onchat, /onchat/groups/:groupId, /onchat/groups/:groupId/chat,
///             /onchat/groups/:groupId/meetups/:meetupId/board,
///             /onchat/groups/:groupId/memories,
///             /onchat/groups/:groupId/settlements/new,
///             /onchat/groups/:groupId/settlements/:settlementId
class OnChatListPage extends StatelessWidget {
  const OnChatListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}
