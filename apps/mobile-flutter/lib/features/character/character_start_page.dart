import 'package:flutter/material.dart';

/// 캐릭터 생성 진입 / route: /character/start  (하단 탭 밖 onboarding route)
/// TODO(캐릭터팀): 이 파일을 캐릭터 생성 시작 화면으로 교체한다.
/// 관련 route: /character/start, /character/appearance, /character/skin-tone,
///             /character/eye-shape, /character/eye-color, /character/hair-color,
///             /character/hair-style, /character/preview, /character/name, /character/complete
class CharacterStartPage extends StatelessWidget {
  const CharacterStartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}
