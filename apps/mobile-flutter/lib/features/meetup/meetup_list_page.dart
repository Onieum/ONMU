import 'package:flutter/material.dart';

/// 약속 탭 — 약속 목록 / route: /meetups
/// TODO(약속팀): 이 파일을 약속 목록 화면으로 교체한다.
/// 관련 route: /meetups, /meetups/new/members, /meetups/new/schedule,
///             /meetups/:meetupId, /meetups/:meetupId/route-review, /meetups/:meetupId/complete
class MeetupListPage extends StatelessWidget {
  const MeetupListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}
