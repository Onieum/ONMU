import 'package:flutter/material.dart';

/// 기록 탭 — OOTD 기록 목록 / route: /ootd/list
/// TODO(기록팀): 이 파일을 기록 목록 화면으로 교체한다.
/// 관련 route: /ootd/new, /ootd/method, /ootd/photo, /ootd/description,
///             /ootd/extra-info, /ootd/style, /ootd/props-mood,
///             /ootd/analysis, /ootd/complete, /ootd/list
class OotdListPage extends StatelessWidget {
  const OotdListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}
