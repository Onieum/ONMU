import 'package:flutter/material.dart';

/// 취향 입력 진입 / route: /preferences/intro  (하단 탭 밖 onboarding route)
/// TODO(취향팀): 이 파일을 취향 입력 화면으로 교체한다.
/// 관련 route: /preferences/intro, /preferences/category, /preferences/food,
///             /preferences/dislike, /preferences/time, /preferences/unavailable-dates,
///             /preferences/summary
class PreferenceIntroPage extends StatelessWidget {
  const PreferenceIntroPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}
